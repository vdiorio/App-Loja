import products from './products.middlewares';
import users from './users.middlewares';
import login from './login.middlewares';
import validation from './token.validation';
import order from './order.middlewares';

export default {
  products,
  users,
  login,
  validation,
  order,
}